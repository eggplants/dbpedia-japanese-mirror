{
  "name": "dbpedia-japanese-sparql-diagram",
  "version": "0.1.0",
  "private": true,
  "description": "Draws the Cloudflare resources Terraform manages as a Mermaid diagram with Cloudflare's own icons",
  "type": "module",
  "scripts": {
    "generate": "puppeteer browsers install chrome-headless-shell && node src/generate.mjs"
  },
  "devDependencies": {
    "@cloudflare/component-icon": "^13.15.3",
    "@mermaid-js/mermaid-cli": "^11.17.0",
    "puppeteer": "^25.10.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "vite-plus": "0.3.1"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^12.0.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18"
  }
}
